[pytest]
testpaths = tests
# Hard ceiling so a hung test fails fast with a stack trace instead of
# wedging the suite. Full suite normally completes in ~20s; 60s leaves
# plenty of headroom for any legitimate slow test.
timeout = 60
